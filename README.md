# 🌤️ Weather App

Погодна аплікація на Rust з використанням Leptos Framework.

## 🚀 Особливості

- ⚡ Швидка та реактивна аплікація на WebAssembly
- 🎨 Сучасний дизайн з адаптивним інтерфейсом
- 🌍 Пошук погоди для будь-якого міста світу
- 📱 Підтримка мобільних пристроїв
- 🔄 Реальний час оновлення даних через OpenWeatherMap API

## 📋 Попередні вимоги

Перед початком переконайтеся, що у вас встановлено:

- Rust (остання стабільна версія)
- Trunk (`cargo install trunk`)
- wasm32-unknown-unknown target (`rustup target add wasm32-unknown-unknown`)

## ⚙️ Налаштування

1. Отримайте безкоштовний API ключ від OpenWeatherMap:
   - Зареєструйтеся на [https://openweathermap.org/api](https://openweathermap.org/api)
   - Скопіюйте ваш API ключ

2. Замініть `YOUR_API_KEY` у файлі `src/api/weather.rs`:
   ```rust
   const API_KEY: &str = "ваш_ключ_тут";
   ```

## 🛠️ Розробка

Запустіть сервер розробки:

```bash
trunk serve
```

Відкрийте браузер на [http://127.0.0.1:8080](http://127.0.0.1:8080)

## 📦 Збірка для production

```bash
trunk build --release
```

Готовий проект буде у директорії `dist/`

## 🌐 Деплой на GitHub Pages

1. Переконайтеся, що у вашому репозиторії включено GitHub Pages:
   - Settings → Pages → Source → GitHub Actions

2. Завантажте код на GitHub:
   ```bash
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/ваше_ім'я/ваш_репозиторій.git
   git push -u origin main
   ```

3. GitHub Actions автоматично створить та опублікує ваш сайт!

## 📁 Структура проекту

```
weather/
├── src/
│   ├── api/
│   │   ├── mod.rs
│   │   └── weather.rs      # API клієнт для OpenWeatherMap
│   ├── components/
│   │   ├── mod.rs
│   │   └── weather_app.rs  # Головний компонент додатку
│   └── main.rs             # Точка входу
├── .github/
│   └── workflows/
│       └── deploy.yml      # GitHub Actions для деплою
├── index.html              # HTML шаблон
├── style.css               # Стилі
├── Cargo.toml             # Залежності Rust
└── Trunk.toml             # Конфігурація Trunk
```

## 🛠️ Технології

- **Leptos** - Реактивний Rust фреймворк
- **WebAssembly** - Високопродуктивний веб-додаток
- **OpenWeatherMap API** - Джерело погодних даних
- **Trunk** - Інструмент збірки для WASM
- **GitHub Pages** - Хостинг

## 📝 Ліцензія

MIT

## 🤝 Внесок

Пул-реквести вітаються! Не соромтеся відкривати issues для пропозицій чи виправлень.
